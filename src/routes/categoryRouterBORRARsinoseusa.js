// import express from "express";
// import {
//   getCategories,
//   createCategory,
//   deleteCategory
// } from "../controllers/categoryController.js";
// import checkAuth from "../middlewares/checkAuth.js";

// const categoryRouter = express.Router();

// categoryRouter.route("/").get(getCategories).post(checkAuth, createCategory);
// categoryRouter.route("/:categoryId").delete(checkAuth, deleteCategory);

// export default categoryRouter;
